{
  "name": "root",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev_server": "lerna run start --scope=backend",
    "dev_client": "lerna run dev --scope=client",
    "dev_build": "lerna run build --scope=client",
    "dev_start": "lerna run start --scope=client",
    "dev_story": "lerna run storybook --scope=client"
  }
}
